---
all:
  children:
    linux_hosts:
      hosts:
        traefik:
        gitlab:
        runner:
  

    docker_hosts:
      hosts:
        traefik:
        gitlab:
        runner:

    proxy:
      hosts:
        traefik:
          ansible_host: 192.168.10.253
          ansible_user: root

    gitlab_servers:
      hosts:
        gitlab:
          ansible_host: 192.168.10.6
          ansible_user: root

    gitlab_runners:
      hosts:
        runner:
          ansible_host: 192.168.10.5
          ansible_user: root
