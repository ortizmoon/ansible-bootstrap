Перед проигрыванием роли, сгенерировать в гитлабе personal access token доступа, с правами API и create_runner
Токен вставить в vault.yml, и запустить роль

# Gitlab runner
Устанавливает и регистрирует gitlab runner в экземпляре гитлаба.

## Порядок действий:

### 1. Cгенерировать в гитлабе `personal access token`, с правами `API` и `Create_runner`

### 2. Положить креды в vault.yml и прогнать роль
